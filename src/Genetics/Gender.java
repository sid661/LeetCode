package Genetics;

public enum Gender {
    FEMALE,MALE
   /* A wrapper class (Integr) for integer performs the following single function:
    given another instance compares the integer value that it stores with one available in
    the passed instance for equality and throws
    'LesserThanException' if the passed value is less than its value and
    'GreaterThanException' if the passed value is greater than its value.*/


/*
    Class IString stores the inverse of a given String. Two instances of IString are considered
    equal if the characters at odd numbered positions in the "inversed strings" are the same.
    Implement the class and a test program.*/
}
